import { ImageTooltip } from './image-tooltip';

describe('ImageTooltip', () => {
  it('should create an instance', () => {
    const directive = new ImageTooltip();
    expect(directive).toBeTruthy();
  });
});
